# 🍽️ Restaurant Management System (Spring Boot API)

Информационная система для управления меню ресторана, построенная на базе Spring Boot и PostgreSQL с применением паттернов проектирования.

## 🛠 Технологический стек
* **Java 17** & **Maven**
* **Spring Boot 3.2.2** (Web, JDBC)
* **PostgreSQL** (База данных)
* **Lombok** (для чистоты кода)

## 🏗 Архитектура проекта
Проект строго следует принципу **Layered Architecture**:
1. **Controller**: Принимает HTTP запросы и отвечает за REST-интерфейс.
2. **Service**: Содержит бизнес-логику приложения.
3. **Repository**: Отвечает за прямое взаимодействие с БД через `JdbcTemplate`.
4. **Model**: Иерархия классов меню (`MenuItem`, `Dish`, `Drink`).

## 💎 Примененные паттерны и принципы
* **Factory Method**: Централизованное создание объектов `Dish` и `Drink` в классе `MenuFactory`.
* **Dependency Injection**: Внедрение зависимостей через конструкторы (Spring IoC).
* **SOLID**: 
    - *SRP*: Каждый класс выполняет строго свою задачу.
    - *OCP*: Система открыта для добавления новых типов блюд без изменения логики контроллера.

## 🚀 Быстрый запуск
1. **База данных**: Создайте таблицу в PostgreSQL:
```sql
CREATE TABLE menu_items (
    id SERIAL PRIMARY KEY,
    type VARCHAR(20) NOT NULL,
    name VARCHAR(100) NOT NULL,
    price DECIMAL(10, 2) NOT NULL,
    extra_info VARCHAR(255)
);
